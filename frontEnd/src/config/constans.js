export const URLBASE = "http://localhost:3000";

export const ENDPOINT = {
  login: `${URLBASE}/login`,
  register: `${URLBASE}/register`,
};

// export const ENDPOINT = {
//   login: "http://localhost:3000/login", // Cambia el puerto al del backend
//   register: "http://localhost:3000/register", // Ruta correcta para el registro
// };
